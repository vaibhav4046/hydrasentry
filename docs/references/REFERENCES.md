# Design references — ArtifactTreeGraph hero

Drop these binary reference files here (you have them locally). Code does NOT import them;
they are visual direction only. The implementation is driven by the storyboard description
baked into the build prompts.

- hydradb-real-tree.png .................. real HydraDB hero (orange voxel tree) — STRUCTURE ONLY, never the orange.
- hydrasentry-current-weak-bg.png ........ current weak hero — what we are replacing.
- hydrasentry-artifact-tree-animation.gif  target animation flow.
- hydrasentry-artifact-tree-poster.png ... static poster frame.
- hydrasentry-artifact-tree-storyboard.png animation states + graph UI + keyframes (primary spec).

Color law: black/white/graphite/silver only. No orange. Risk = brighter white + glow + dashed motion + contrast.