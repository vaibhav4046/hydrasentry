# Convergence Round Log

Each round: honest 8-axis self-score, the single highest-impact gap picked, and the merge/revert result.

Round 1: realness=9 depth=9 hardening=9 standards=7 usability=8 polish=9 security=9 narrative=9 | gap: OWASP ASI06 control mapping existed only as README prose - turned it into a verified, machine-readable, served artifact (backend/standards/asi06.py single source of truth + GET /standards/asi06 endpoint + test that asserts every cited implementing file/symbol actually exists, so the mapping cannot rot into a false claim) | result: merged (checkpoint-round1; backend 182 pass + 6 skip, frontend build green, judge-demo 87/HIGH/block intact)
Round 2: realness=9 depth=9 hardening=9 standards=9 usability=9 polish=9 security=9 narrative=9 | gap: the self-verified ASI06 mapping from round 1 was served only at GET /standards/asi06 and asserted by tests - a judge had to know the curl URL, so it was invisible in the live product. Added an in-app /standards page (frontend/app/standards/page.tsx) that fetches and renders the verified mapping, each control showing its real implementing module+symbol and the backend-recomputed verified flag, with honest loading/error/offline states (offline returns verified_all=null and shows VERIFICATION OFFLINE, never a fake green tick); nav entry under SECURITY; backend test pins the exact response fields the page reads so a rename cannot silently blank the surface. This lifts BOTH laggards: standards (the compliance artifact is now a visible product surface, not just an endpoint) and usability (a judge sees OWASP coverage with no login and no curl) | result: merged (checkpoint-round2; backend 183 pass + 6 skip, frontend build green incl. /standards prerendered + TS pass, judge-demo 87/HIGH on deployed backend intact)
Round 3 (close-out): all 8 axes >=9 (realness 9, depth 9, hardening 9, standards 9, usability 9, polish 9, security 9, narrative 9). No qualifying non-human gap remains; loop at exit. Housekeeping: committed the Remotion source (ArtifactTree.tsx, HydraArtifactTreeSequence.tsx) that generated the committed constellan_film.mp4 and master-cut intro/outro, so main is fully clean and the video is reproducible from source. tsc exit 0; live judge-demo 87/HIGH/block; /standards 200.
Push round 2 (target security): realness=9 depth=9.5 hardening=9 standards=9 usability=9 polish=9 security=9 narrative=9 | gap: red-team finding #11 (open since Phase 2) -- `main.py` wired `allow_origins=settings.cors_origins or ["*"]` with `allow_credentials=True`, so an unset `CORS_ORIGINS` asked the framework for a credentialed wildcard (a CORS-spec contradiction the deployed surface only survived because the framework silently downgrades it; the policy was unsafe-by-config, not safe-by-design). Added `config.resolve_cors()` as the single source of truth: explicit allowlist -> those origins + credentials; no allowlist but a concrete `FRONTEND_URL` -> that one origin + credentials; nothing configured -> `*` with `allow_credentials` forced OFF. `main.py` now wires the resolved policy and `/config/status` (signed-in only) surfaces the effective `allow_origins`/`allow_credentials`/`is_wildcard` so an auditor can confirm no credentialed wildcard is in force. 13 new tests incl. a parametrised invariant (never `is_wildcard and allow_credentials` for any env) + a real preflight asserting the running app never echoes `ACAO:*` with `ACAC:true`. Closes BACKLOG #11 and the CORS half of #21 | result: merged (checkpoint-push2; backend 203 pass + 7 skip [+13 CORS tests], frontend build green incl. /standards, live judge-demo 87/HIGH/block intact, ota_packs restored)
Push round 3 (target standards): realness=9 depth=9.5 hardening=9 standards=9 usability=9 polish=9 security=9.5 narrative=9 | gap: the self-verified standards artifact covered only ONE OWASP risk (ASI06 Memory Poisoning), so the compliance surface a judge/auditor sees spoke to a single threat while the product actually implements controls across the whole OWASP Agentic Security Initiative Top-10. Broadened it into a full self-verifying ASI Top-10 coverage map: `backend/standards/asi.py` (single source) + `GET /standards/asi` + an in-app section on `/standards` leading with the Top-10 grid above the ASI06 detail. Each of the 10 risks is honestly marked covered / partial / out_of_scope (8 covered, 1 partial, 1 out-of-scope); covered/partial rows name a REAL implementing module+symbol and the backend recomputes verification against its own running code; out_of_scope rows carry NO evidence. `test_standards_asi.py` enforces honesty in BOTH directions (covered/partial must cite real code; out_of_scope must carry none) and pins that `verified_all` reacts (flips False when a covered symbol is removed) so it is recomputed, not hardcoded -- this means a true verified_all certifies both real coverage AND that no risk is inflated with borrowed proof. The ASI06 row reuses the verified asi06 sub-controls verbatim so the two surfaces cannot disagree. Additive + read-only: no value-path imports, judge-demo 87/HIGH untouched by an in-suite assertion | result: merged (checkpoint-push3; backend 215 pass + 7 skip [+12 ASI Top-10 tests], frontend build green incl. /standards prerendered + lint + TS pass, live judge-demo 87/HIGH/block intact, ota_packs restored)
Push round 1 (target depth): realness=9 depth=9 hardening=9 standards=9 usability=9 polish=9 security=9 narrative=9 | gap: the semantic detector's accuracy was ASSERTED not measured in-repo - the docstring claimed "measured separation on a held-out set" and the README claimed paraphrase detection, but no committed, runnable harness computed precision/recall/F1 over a labeled poison/benign set using the detector's OWN decision gate. Added backend/eval/ a versioned labeled dataset (poison paraphrases + benign policy-affirming/operational, each with provenance) and a deterministic harness that runs the REAL detect() gate over it and reports a confusion matrix (precision/recall/F1/accuracy), plus a test that pins minimum metrics so a threshold regression fails CI. Offline via the same fake-embedding space the suite already uses (no key, runs in CI); an opt-in live mode re-runs it against real gemini-embedding-001. README detection claim now cites the measured numbers instead of an unbacked separation claim | result: merged (checkpoint-push1; backend 190 pass + 7 skip [+7 offline eval tests, +1 live skip], frontend build green incl. /standards, live judge-demo 87/HIGH/block intact; offline measured precision=recall=F1=1.000 over 25 labeled rows; ota_packs restored)
